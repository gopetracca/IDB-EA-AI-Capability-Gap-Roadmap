[project]
name = "idb-ea-ai-capability-gap-roadmap"
version = "0.2.0"
description = "IDB Enterprise Architecture - AI capability map, assessment and roadmap. Facts in facts/, rules in scales/, views generated into out/."
readme = "README.md"
requires-python = ">=3.13"
dependencies = ["openpyxl>=3.1"]

[tool.setuptools]
packages = []
